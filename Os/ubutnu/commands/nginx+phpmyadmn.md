Nginx पर सेटअप करने का **DevOps तरीका** है:

### स्टेप 1: इनस्टॉल करना

टर्मिनल में यह कमांड चलाएं:

```bash
sudo apt update
sudo apt install phpmyadmin

```

⚠️ **महत्वपूर्ण मोड़:**
इन्स्टॉलेशन के दौरान एक स्क्रीन आएगी जो पूछेगी: *"Please choose the web server to configure automatically."*
वहाँ आपको `apache2` और `lighttpd` के आप्शन दिखेंगे।

* चूँकि वहां **Nginx** का नाम नहीं होगा, आपको **TAB** दबाकर **Ok** पर जाना है या **ESC** दबाना है (किसी को सेलेक्ट नहीं करना है)।
* आगे वह `dbconfig-common` के लिए पूछेगा, उसे **Yes** कर दें और पासवर्ड सेट कर लें।

### स्टेप 2: Nginx से कनेक्ट करना (Symlink बनाना)

phpMyAdmin असल में `/usr/share/phpmyadmin` फोल्डर में इनस्टॉल होता है। Nginx को इसके बारे में पता नहीं होता। हमें Nginx के पब्लिक फोल्डर में इसका एक शॉर्टकट (Symlink) बनाना होगा।

यह कमांड चलाएं:

```bash
sudo ln -s /usr/share/phpmyadmin /var/www/html/phpmyadmin

```

*(इसका मतलब: `/usr/share` से फाइल उठाओ और `/var/www/html` में `phpmyadmin` नाम से शॉर्टकट बना दो।)*

### स्टेप 3: टेस्ट करना

अब आप अपने ब्राउज़र में जाकर चेक कर सकते हैं:
`http://your_server_ip/phpmyadmin`
या
`http://your_domain.com/phpmyadmin`

---

### 🛡️ DevOps सुरक्षा टिप (Security Tip)

एक DevOps इंजीनियर के तौर पर, हम कभी भी `/phpmyadmin` URL को डिफ़ॉल्ट नहीं छोड़ते, क्योंकि हैकर्स सबसे पहले यही URL चेक करते हैं।

आप अपनी सुरक्षा बढ़ाने के लिए सिम्लिंक (Symlink) का नाम बदल सकते हैं:

1. पुराना लिंक हटाएं (अगर बना है): `sudo rm /var/www/html/phpmyadmin`
2. नया गुप्त नाम दें:
```bash
sudo ln -s /usr/share/phpmyadmin /var/www/html/gupt-database

```



अब आपका डेटाबेस `domain.com/phpmyadmin` पर नहीं, बल्कि `domain.com/gupt-database` पर खुलेगा। हैकर्स को यह कभी नहीं मिलेगा! 😎

### 💡 एक बेहतर विकल्प (DevOps पसंद)

आजकल कई DevOps इंजीनियर्स phpMyAdmin की जगह **Adminer** का उपयोग करते हैं।

* यह सिर्फ **एक सिंगल PHP फाइल** होती है।
* इसे इनस्टॉल नहीं करना पड़ता, बस `adminer.php` डाउनलोड करके अपने `public` फोल्डर में डाल दीजिये और यह चल पड़ता है।
* यह phpMyAdmin से बहुत हल्का और सुरक्षित है।

क्या आप **Adminer** को ट्राई करना चाहेंगे या phpMyAdmin के साथ ही आगे बढ़ना चाहते हैं?