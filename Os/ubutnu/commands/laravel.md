
---

# 📜 Linux Cheatsheet: Laravel डेवलपर्स के लिए

यह चीटशीट विशेष रूप से उन कमांड्स पर केंद्रित है जो आपको एक Laravel प्रोजेक्ट को Linux (Ubuntu/Debian) सर्वर पर डिप्लॉय और मैनेज करने के लिए चाहिए।

---

## 1. नेविगेशन (Navigation) - इधर-उधर जाना

सर्वर पर फोल्डर्स के बीच घूमने के लिए।

| कमांड | विवरण (Description) | उदाहरण |
| --- | --- | --- |
| `pwd` | अभी आप किस फोल्डर में हैं, उसका पूरा पथ (path) दिखाता है। | `pwd` |
| `ls` | फोल्डर के अंदर की फाइल्स की लिस्ट दिखाता है। | `ls` |
| `ls -la` | **बहुत जरुरी:** छुपी हुई फाइल्स (जैसे `.env`) और परमिशन भी दिखाता है। | `ls -la` |
| `cd <folder>` | किसी फोल्डर के अंदर जाने के लिए। | `cd /var/www/html` |
| `cd ..` | एक फोल्डर पीछे (back) जाने के लिए। | `cd ..` |
| `cd ~` | अपने होम (Home) डायरेक्टरी में जाने के लिए। | `cd ~` |

---

## 2. फाइल और फोल्डर मैनेजमेंट (File Operations)

फाइल बनाना, डिलीट करना या कॉपी करना।

| कमांड | विवरण | उदाहरण |
| --- | --- | --- |
| `mkdir <name>` | नया फोल्डर बनाने के लिए। | `mkdir new_project` |
| `touch <name>` | नई खाली फाइल बनाने के लिए। | `touch test.txt` |
| `cp <source> <dest>` | फाइल कॉपी करने के लिए। (जैसे `.env.example` से `.env` बनाना)। | `cp .env.example .env` |
| `mv <old> <new>` | फाइल का नाम बदलने या मूव (Cut-Paste) करने के लिए। | `mv old.txt new.txt` |
| `rm <file>` | फाइल डिलीट करने के लिए। | `rm error.log` |
| `rm -rf <folder>` | **सावधानी:** पूरा फोल्डर और उसके अंदर का सब कुछ डिलीट करने के लिए। | `rm -rf my_project` |

---

## 3. परमिशन और ओनरशिप (Permissions & Ownership)

**Laravel के लिए सबसे महत्वपूर्ण सेक्शन।** जब "Permission Denied" या "500 Server Error" आए।

| कमांड | विवरण | उपयोग (Laravel के लिए) |
| --- | --- | --- |
| `chmod -R 775 <folder>` | रीड/राइट परमिशन सेट करना। | `sudo chmod -R 775 storage` |
| `chown -R user:group <folder>` | फोल्डर का मालिक (Owner) बदलना। | `sudo chown -R www-data:www-data /var/www/html/project` |

> **Pro Tip:** Laravel में `storage` और `bootstrap/cache` फोल्डर्स को हमेशा लिखने की अनुमति (Writable permission) चाहिए होती है।

---

## 4. फाइल एडिट करना (Editing Files - Nano)

सर्वर पर `.env` या कॉन्फिगरेशन फाइल बदलने के लिए।

* **फाइल खोलें:** `nano .env`
* **सेव करें:** `Ctrl + O` फिर `Enter` दबाएं।
* **बाहर निकलें (Exit):** `Ctrl + X`

---

## 5. सर्वर मैनेजमेंट (Service Management)

जब आप PHP की सेटिंग बदलें या कोड डिप्लॉय करें।

| कमांड | विवरण | उदाहरण |
| --- | --- | --- |
| `sudo systemctl restart nginx` | Nginx वेब सर्वर को रीस्टार्ट करने के लिए। | जब Nginx config में बदलाव करें। |
| `sudo systemctl restart php8.x-fpm` | PHP प्रोसेस को रीस्टार्ट करने के लिए (x की जगह अपना वर्जन लिखें)। | `sudo systemctl restart php8.2-fpm` |
| `sudo systemctl status <service>` | चेक करें कि सर्विस चल रही है या बंद हो गई। | `sudo systemctl status nginx` |

---

## 6. डिप्लॉयमेंट और Git (Deployment)

कोड को सर्वर पर लाने के लिए।

| कमांड | विवरण |
| --- | --- |
| `git clone <url>` | पहली बार प्रोजेक्ट को सर्वर पर डाउनलोड करने के लिए। |
| `git pull origin main` | GitHub से नया कोड (Changes) सर्वर पर लाने के लिए। |
| `composer install --no-dev --optimize-autoloader` | **Production** के लिए डिपेंडेंसी इनस्टॉल करना (तेज़ और सुरक्षित)। |
| `php artisan migrate --force` | डाटाबेस माइग्रेशन चलाने के लिए (बिना "Yes" पूछे)। |
| `php artisan config:cache` | `.env` में बदलाव के बाद कैश अपडेट करने के लिए। |
| `php artisan route:cache` | रूट्स को फास्ट करने के लिए। |

---

## 7. लॉग्स चेक करना (Debugging Logs)

जब वेबसाइट न चले, तो यहाँ देखें कि गलती क्या है।

* **लाइव एरर देखें (Real-time):**
`tail -f storage/logs/laravel.log`
*(जैसे-जैसे एरर आएगी, यह स्क्रीन पर अपने आप अपडेट होगा। बाहर निकलने के लिए `Ctrl + C` दबाएं)*
* **Nginx एरर देखें:**
`tail -f /var/log/nginx/error.log`

---

## 8. कुछ अन्य जरूरी कमांड्स (Utilities)

* `sudo apt update`: अपने सर्वर की पैकेज लिस्ट अपडेट करना।
* `sudo apt install <package>`: कोई नया सॉफ्टवेर डालना (जैसे `sudo apt install unzip`)।
* `history`: आपने पिछली जो भी कमांड्स टाइप की हैं, उनकी लिस्ट देखना।
* `grep "text" file`: किसी फाइल के अंदर कुछ खोजना। (जैसे: `grep "APP_DEBUG" .env`)

---

### इसका अभ्यास कैसे करें?

मेरा सुझाव है कि आप इस लिस्ट को प्रिंट कर लें या अपने फ़ोन में सेव कर लें। जब भी आप प्रैक्टिस करें, कोशिश करें कि फाइल मैनेजर (GUI) का इस्तेमाल न करें, बल्कि इन कमांड्स का उपयोग करें।