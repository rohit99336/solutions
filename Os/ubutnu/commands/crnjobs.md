
---

# ⏰ क्रॉन जॉब्स चीटशीट: ऑटोमेशन मास्टर (Cron Jobs Cheatsheet)

### 1. क्रॉन जॉब क्या है? (अवधारणा)

क्रॉन (Cron) लिनक्स का एक **समय-आधारित जॉब शेड्यूलर** (Time-Based Job Scheduler) है।
इसका सिद्धांत बहुत सीधा है: **"कब चलाना है?"** + **"क्या चलाना है?"**
अगर आप चाहते हैं कि कोई स्क्रिप्ट रोज रात को चले, तो क्रॉन यह काम करेगा, भले ही आप सो रहे हों।

---

### 2. क्रॉन टेबल को मैनेज करना

सबसे पहले इन कमांड्स को याद कर लें:

| कमांड | विवरण |
| --- | --- |
| `crontab -e` | **एडिट (Edit):** नया क्रॉन जॉब जोड़ने या पुराना हटाने के लिए। (जब आप इसे पहली बार चलाएंगे, तो यह पूछेगा कि कौन सा एडिटर चाहिए, `1` दबाकर Nano चुनें)। |
| `crontab -l` | **लिस्ट (List):** यह देखने के लिए कि अभी सिस्टम में कौन-कौन से क्रॉन जॉब्स सेट हैं। |
| `crontab -r` | **रिमूव (Remove):** सारे क्रॉन जॉब्स को एक साथ डिलीट करने के लिए (इसे सावधानी से उपयोग करें)। |

---

### 3. सिंटेक्स का जादू (5 सितारे)

क्रॉन में 5 सितारे (asterisks) होते हैं `* * * * *`। हर सितारे का अपना मतलब होता है:

```bash
* * * * * command_to_execute
| | | | |
| | | | ----- हफ्ते का दिन (0 - 6) (रविवार=0, सोमवार=1...)
| | | ------- महीना (1 - 12)
| | --------- महीने का दिन (1 - 31)
| ----------- घंटा (0 - 23) (24-घंटे का फॉर्मेट)
------------- मिनट (0 - 59)

```

**याद रखने के लिए शॉर्टकट:**

* `*` = हर (Every) - जैसे हर मिनट, हर घंटे।
* `/` = अंतराल (Interval) - जैसे `*/5` का मतलब "हर 5 मिनट में"।
* `,` = एक से ज्यादा (Multiple) - जैसे `Mon,Wed` (सोमवार और बुधवार)।

---

### 4. असली दुनिया के उदाहरण (Laravel और DevOps के लिए)

यहाँ वो सबसे जरूरी उदाहरण हैं जो एक Laravel डेवलपर के तौर पर आपको चाहिए होंगे:

#### A. लारवेल शेड्यूलर (Laravel Scheduler - सबसे जरूरी)

Laravel में हम `Kernel.php` फाइल में शेड्यूलिंग लिखते हैं, लेकिन उसे चलाने के लिए सर्वर पर **सिर्फ एक क्रॉन** लगाना पड़ता है जो हर मिनट चलता है।

```bash
* * * * * cd /var/www/html/project && php artisan schedule:run >> /dev/null 2>&1

```

*(इसका हिंदी में मतलब: हर मिनट प्रोजेक्ट फोल्डर में जाओ और artisan कमांड चलाओ। जो भी आउटपुट आये उसे सेव मत करो, बस चलने दो।)*

#### B. डेटाबेस का ऑटोमैटिक बैकअप (Database Backup)

मान लीजिये आप चाहते हैं कि **रोज रात 2:30 बजे** आपके डेटाबेस का बैकअप बन जाए।

```bash
30 2 * * * mysqldump -u root -pPASSWORD dbname > /home/rohit/backups/db_backup.sql

```

* `30` = 30वां मिनट
* `2` = 2 AM (रात के 2 बजे)
* `* * *` = रोजाना, हर महीने।

#### C. ऑटो-डिप्लॉयमेंट स्क्रिप्ट (Auto-Deployment)

आपने जो `deploy.sh` स्क्रिप्ट बनाई थी, अगर आप चाहते हैं कि वो **हर रविवार सुबह 5 बजे** अपने आप चल जाए:

```bash
0 5 * * 0 /home/rohit/deploy.sh

```

* `0` = 0 मिनट
* `5` = 5 AM (सुबह के 5 बजे)
* `0` = रविवार (Sunday)

---

### 5. कुछ उपयोगी शॉर्टकट्स (Common Timings)

अगर आप `* * *` में कंफ्यूज हो रहे हैं, तो लिनक्स कुछ शब्दों को सीधे समझता है:

| कीवर्ड (Keyword) | मतलब | कब चलेगा? |
| --- | --- | --- |
| `@daily` | रोजाना रात 12 बजे | `0 0 * * *` |
| `@hourly` | हर घंटे की शुरुआत में | `0 * * * *` |
| `@weekly` | हफ्ते में एक बार (रविवार) | `0 0 * * 0` |
| `@reboot` | **बहुत महत्वपूर्ण:** सर्वर रीस्टार्ट होने पर | जैसे ही सर्वर चालू (ON) होगा, यह कमांड एक बार चलेगी। |

**उदाहरण (@reboot):**
अगर आप चाहते हैं कि सर्वर स्टार्ट होते ही **Queue Worker** अपने आप चालू हो जाए:

```bash
@reboot php /var/www/html/project/artisan queue:work

```

---

### 6. डिबगिंग और लॉग्स (DevOps प्रो टिप) 🛑

क्रॉन जॉब्स बैकग्राउंड (पर्दे के पीछे) में चलते हैं, तो अगर कोई एरर आया तो आपको स्क्रीन पर नहीं दिखेगा। इसलिए हम **लॉग्स (Logs)** सेव करते हैं।

**गलत तरीका:**
`* * * * * script.sh` (पता नहीं चलेगा कि स्क्रिप्ट फेल हुई या पास)

**सही तरीका (लॉग फाइल बनाना):**

```bash
* * * * * /path/to/script.sh >> /var/log/my_cron.log 2>&1

```

* `>>` : इसका मतलब है कि नया डेटा फाइल में **जोड़ें** (पुराना डेटा डिलीट नहीं होगा)।
* `2>&1` : यह एक तकनीकी कोड है। इसका मतलब है कि **सफलता का मैसेज** और **एरर मैसेज** दोनों लॉग फाइल में सेव होंगे।

---

### सारांश (Summary)

अब आप समझ गए होंगे कि DevOps इंजीनियर रात को चैन की नींद कैसे सोते हैं - क्योंकि उनका काम **Cron** कर रहा होता है।

1. **बैकअप:** अपने आप हो रहा है।
2. **ईमेल भेजना:** Queue Worker से हो रहा है।
3. **मेंटेनेंस:** रात को अपने आप हो जाता है।