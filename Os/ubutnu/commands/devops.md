
---

# 🚀 Advanced Linux Cheatsheet: For DevOps & Software Engineers

यह चीटशीट आपको सिस्टम के **अंदर (Internal)** झांकने और समस्याओं को **Diagnose (निदान)** करने में मदद करेगी।

## 1. सिस्टम मॉनिटरिंग (System Performance Monitoring)

*सर्वर स्लो क्यों है? CPU या RAM कौन खा रहा है?*

| कमांड | विवरण | DevOps उपयोग (Use Case) |
| --- | --- | --- |
| `htop` | `top` का एडवांस वर्शन। कलरफुल इंटरफेस में CPU/RAM यूसेज दिखाता है। | जब सर्वर धीमा हो, तो चेक करें कि क्या `php-fpm` या `mysql` CPU को 100% यूज़ कर रहा है। |
| `free -h` | RAM (Memory) का स्टेटस दिखाता है (GB/MB में)। | चेक करें कि कहीं RAM फुल तो नहीं हो गई? 'Swap' मेमोरी यूज़ हो रही है या नहीं। |
| `df -h` | डिस्क स्पेस (Hard Disk) चेक करना। | अगर `Disk Full` का एरर आये, तो चेक करें किस पार्टीशन में जगह कम है। |
| `du -sh *` | फोल्डर का साइज चेक करना। | यह पता लगाने के लिए कि कौन सा फोल्डर (जैसे `storage/logs`) सबसे ज्यादा जगह घेर रहा है। |
| `uptime` | सर्वर कितनी देर से चल रहा है और लोड एवरेज क्या है। | सर्वर रीस्टार्ट हुआ था या नहीं, यह जानने के लिए। |

---

## 2. नेटवर्किंग और कनेक्टिविटी (Networking & Connectivity)

*क्या मेरी API काम कर रही है? क्या पोर्ट ओपन है?*

| कमांड | विवरण | DevOps उपयोग (Use Case) |
| --- | --- | --- |
| `curl -v <url>` | कमांड लाइन से URL हिट करना (Header डिटेल्स के साथ)। | ब्राउज़र के बिना API चेक करना। HTTP Status Code (200, 404, 500) देखने के लिए। |
| `ping <ip>` | सर्वर तक पहुँच (Reachability) चेक करना। | क्या गूगल या डेटाबेस सर्वर ऑनलाइन है? |
| `netstat -tulnp` | ओपन पोर्ट्स की लिस्ट देखना। | चेक करें कि क्या Nginx सच में `Port 80` पर सुन रहा है या MySQL `3306` पर एक्टिव है। |
| `ss -tulnp` | `netstat` का मॉडर्न और तेज वर्शन। | वही काम करता है, पोर्ट्स चेक करना। |
| `nslookup <domain>` | DNS रिकॉर्ड चेक करना। | अगर डोमेन कनेक्ट नहीं हो रहा, तो चेक करें कि वह सही IP पर पॉइंट कर रहा है या नहीं। |
| `ufw status` | फायरवॉल का स्टेटस (Uncomplicated Firewall)। | कौन से पोर्ट्स बाहर की दुनिया के लिए खुले हैं (Allow) और कौन से बंद (Deny)। |

---

## 3. प्रोसेस मैनेजमेंट (Process Management)

*कोई प्रोसेस अटक गई है तो उसे कैसे मारें (Kill)?*

| कमांड | विवरण | DevOps उपयोग (Use Case) |
| --- | --- | --- |
| `ps aux` | चल रही सभी प्रोसेस की डिटेल लिस्ट। | किसी स्पेसिफिक प्रोसेस की ID (PID) ढूँढने के लिए। |
| `ps aux | grep php` | सिर्फ PHP से जुडी प्रोसेस ढूँढना। |
| `kill <PID>` | किसी प्रोसेस को बंद करना (ID के द्वारा)। | अगर कोई स्क्रिप्ट अटक गयी है, तो उसे बंद करने के लिए। |
| `kill -9 <PID>` | फोर्स किल्ल (Force Kill)। | जब नार्मल `kill` से प्रोसेस बंद न हो रही हो (ब्रह्मास्त्र)। |
| `nohup <command> &` | प्रोसेस को बैकग्राउंड में चलाना। | अगर आप SSH बंद भी कर दें, तो भी वह कमांड चलती रहेगी (जैसे लम्बा import script)। |

---

## 4. एडवांस फाइल सर्च और लॉग्स (Advanced Search & Logs)

*लाखों फाइलों में से एक लाइन ढूँढना।*

| कमांड | विवरण | DevOps उपयोग (Use Case) |
| --- | --- | --- |
| `find / -name "file.txt"` | पूरे सिस्टम में फाइल ढूँढना। | जब आपको याद न हो कि आपने `nginx.conf` कहाँ रखी थी। |
| `grep -r "error" .` | फोल्डर के अंदर सभी फाइलों में "टेक्स्ट" खोजना। | पूरे प्रोजेक्ट में यह ढूँढना कि "Call to undefined function" कहाँ लिखा है। |
| `tail -f -n 100 <log>` | लॉग फाइल की आखिरी 100 लाइन्स लाइव देखना। | प्रोडक्शन एरर को रियल टाइम में देखने के लिए। |
| `history | grep "artisan"` | आपने पहले `artisan` से जुडी क्या कमांड चलाई थी, उसे खोजना। |

---

## 5. फाइल ट्रांसफर और आर्काइव (Archive & Transfer)

*बैकअप लेना और फाइल भेजना।*

| कमांड | विवरण | DevOps उपयोग (Use Case) |
| --- | --- | --- |
| `tar -czvf name.tar.gz <folder>` | फोल्डर को कंप्रेस (Zip) करना। | प्रोजेक्ट का बैकअप लेने के लिए (साइज छोटा हो जाता है)। |
| `tar -xzvf file.tar.gz` | कंप्रेस फाइल को एक्सट्रैक्ट (Unzip) करना। | बैकअप को रिस्टोर करने के लिए। |
| `scp file user@ip:/path` | एक सर्वर से दूसरे सर्वर पर फाइल भेजना (Secure Copy)। | अपने लोकल कंप्यूटर से फाइल सर्वर पर भेजने के लिए (बिना Git के)। |
| `wget <url>` | इंटरनेट से फाइल डाउनलोड करना। | सर्वर पर कोई फाइल सीधे डाउनलोड करने के लिए। |

---

## 6. यूजर मैनेजमेंट (User Management)

*टीम के लिए एक्सेस मैनेज करना।*

| कमांड | विवरण | DevOps उपयोग (Use Case) |
| --- | --- | --- |
| `whoami` | अभी आप कौन से यूजर हैं? | परमिशन चेक करने से पहले यह देखना जरुरी है। |
| `sudo useradd <name>` | नया यूजर बनाना। | अपनी टीम के किसी मेंबर के लिए सर्वर एक्सेस बनाना। |
| `passwd <user>` | किसी यूजर का पासवर्ड बदलना। | पासवर्ड रिसेट करने के लिए। |

---

### 💡 DevOps Pro Tip: पाइपिंग (`|`) का जादू

DevOps में हम अक्सर एक कमांड के आउटपुट को दूसरी कमांड में भेजते हैं। इसे "Piping" कहते हैं।

**उदाहरण:**
मान लीजिये आपको देखना है कि क्या `php` चल रहा है?
`ps aux | grep php`
(यहाँ `ps aux` ने सब कुछ लिस्ट किया, और `grep` ने उसमें से सिर्फ `php` वाली लाइन छांट दी।)

---

Gemini chat url -  https://gemini.google.com/share/b7c82591cbc6 for Devops