
---

# 🔒 SSL (HTTPS) सेटअप गाइड: साइट को सुरक्षित बनाना

### स्टेप 1: सर्टबॉट और Nginx प्लगइन इनस्टॉल करना

सबसे पहले सर्वर पर Certbot टूल डालना होगा। टर्मिनल में ये कमांड्स चलाएं:

```bash
sudo apt update
sudo apt install certbot python3-certbot-nginx

```

### स्टेप 2: SSL सर्टिफिकेट जनरेट करना

अब हम सर्टबॉट को बोलेंगे कि वह हमारे डोमेन के लिए सर्टिफिकेट लाए और Nginx को कॉन्फ़िगर करे।
*(ध्यान रहे: इस स्टेप से पहले आपका डोमेन नेम सही IP एड्रेस पर पॉइंट होना चाहिए)*।

कमांड (`example.com` की जगह अपना डोमेन लिखें):

```bash
sudo certbot --nginx -d example.com -d www.example.com

```

### स्टेप 3: सवालों के जवाब देना (Configuration)

जब आप ऊपर वाली कमांड चलाएंगे, तो सर्टबॉट आपसे कुछ सवाल पूछेगा। ध्यान से जवाब दें:

1. **Enter email address:** अपना असली ईमेल डालें (ताकि सर्टिफिकेट एक्सपायर होने से पहले आपको मेल आ सके)।
2. **Terms of Service:** `A` दबाकर Enter करें (Agree/सहमत होने के लिए)।
3. **Share Email:** `N` दबाएं (अगर आपको मार्केटिंग ईमेल नहीं चाहिए)।
4. **Redirect HTTP to HTTPS (सबसे महत्वपूर्ण):**
* वह पूछेगा कि क्या ट्रैफिक को रिडायरेक्ट करना है?
* आप **`2`** चुनें और Enter दबाएं।
* इससे अगर कोई `http://example.com` खोलेगा, तो वह अपने आप सुरक्षित `https://example.com` पर चला जाएगा।



### स्टेप 4: वेरिफिकेशन (जादू देखें) 🔒

अब अपने ब्राउज़र में अपनी वेबसाइट खोलें। आपको एड्रेस बार में एक **ताला (Lock Icon)** दिखाई देगा।
बधाई हो! आपकी साइट अब एनक्रिप्टेड (Encrypted) और सुरक्षित है।

### स्टेप 5: ऑटो-रिन्यूअल चेक करना (DevOps प्रो टिप)

Let's Encrypt के सर्टिफिकेट सिर्फ **90 दिन** (3 महीने) के लिए वैलिड होते हैं। लेकिन अच्छी बात यह है कि सर्टबॉट ने इनस्टॉल होते वक्त एक **Cron Job** (जो हमने अभी सीखा था) पहले ही सेट कर दिया है जो इसे खुद रिन्यू (Renew) करेगा।

बस एक बार टेस्ट कर लें कि रिन्यूअल प्रोसेस सही चल रहा है या नहीं:

```bash
sudo certbot renew --dry-run

```

*(अगर आउटपुट में "Congratulations, all simulated renewals succeeded" आए, तो इसका मतलब सब कुछ सही है। आपको अब कभी सर्टिफिकेट की चिंता नहीं करनी पड़ेगी)।*

---