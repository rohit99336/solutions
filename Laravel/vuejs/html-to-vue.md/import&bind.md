स्पष्ट रोडमैप देता है कि Inertia + Vue सेटअप में इन्हें कैसे इंटीग्रेट करना है। फ़ाइलों को सफलतापूर्वक माइग्रेट करने के लिए, हम उन्हें तीन मुख्य स्थानों में व्यवस्थित करेंगे:

1.  **प्लेसमेंट (Placement):** फ़ाइलों को `resources/` फ़ोल्डर में कॉपी करना।
2.  **बंडलिंग (Bundling):** उन्हें `resources/js/app.js` में Import करना।
3.  **कॉन्फ़िगरेशन (Configuration):** jQuery और Bootstrap को Global बनाना।

यहाँ चरण-दर-चरण प्रक्रिया दी गई है:

-----

## 🛠️ स्टेप-बाय-स्टेप माइग्रेशन गाइड

मान लें कि आपका HTML टेम्पलेट फ़ोल्डर `assets/` में ये फ़ाइलें रखता है।

### स्टेप 1: एसेट फ़ाइलें कॉपी करें

सबसे पहले, `assets/` फ़ोल्डर से सभी फ़ाइलों को अपने Laravel प्रोजेक्ट के `resources/` फ़ोल्डर में कॉपी करें।

| मूल फ़ोल्डर (Original) | गंतव्य (Destination) |
| :--- | :--- |
| `assets/css/*` | `resources/css/` |
| `assets/js/*` | `resources/js/vendor/` |

**फ़ोल्डर संरचना इस प्रकार होनी चाहिए:**

```
resources/
├── css/
│   ├── bootstrap.min.css
│   ├── animate.css
│   ├── all.min.css
│   ├── swiper.min.css
│   ├── lightcase.css
│   └── style.css       <-- आपकी मुख्य थीम CSS
└── js/
    └── vendor/
        ├── jquery-3.6.0.min.js
        ├── bootstrap.bundle.min.js
        ├── modernizr-3.11.2.min.js
        ├── isotope.pkgd.min.js
        ├── swiper.min.js
        ├── // ... other JS files
        ├── plugins.js
        └── main.js     <-- आपका कस्टम JS लॉजिक
```

### स्टेप 2: CSS फ़ाइलों को बंडल करें (`resources/js/app.js` या `resources/css/app.css`)

आपकी सभी CSS फ़ाइलें अब `resources/css` में हैं। उन्हें लोड करने के लिए, आप अपनी मुख्य CSS एंट्री फ़ाइल (`resources/css/app.css`) का उपयोग करें:

```css
/* resources/css/app.css */

/* 1. Base files (Bootstrap, Animate) */
@import "./bootstrap.min.css";
@import "./animate.css";
@import "./all.min.css"; /* FontAwesome/Icons */

/* 2. Plugin CSS */
@import "./swiper.min.css";
@import "./lightcase.css";

/* 3. Theme's main stylesheet (always load last for custom overrides) */
@import "./style.css";
```

और सुनिश्चित करें कि `resources/js/app.js` इस `app.css` को Import कर रहा है:

```javascript
// resources/js/app.js
import '../css/app.css'; // <-- यह आपकी सभी CSS फ़ाइलों को बंडल कर देगा
// ...
```

### स्टेप 3: JavaScript फ़ाइलों को कॉन्फ़िगर और बंडल करें

JS फ़ाइलों को **सही क्रम** में और **Global कॉन्फ़िगरेशन** के साथ Import करना आवश्यक है।

#### A. Global कॉन्फ़िगरेशन (`resources/js/bootstrap.js`)

jQuery और Bootstrap जैसे पैकेजेस को Global बनाने के लिए `bootstrap.js` को अपडेट करें। (यदि आप `jquery-3.6.0.min.js` को सीधे `vendor` फ़ोल्डर से उपयोग कर रहे हैं, तो इसे `app.js` में सबसे पहले Import करना होगा)।

**सरल दृष्टिकोण:**

1.  सुनिश्चित करें कि आपके प्रोजेक्ट में `jquery` npm पैकेज स्थापित नहीं है, और आप केवल अपनी `vendor/jquery-3.6.0.min.js` फ़ाइल का उपयोग कर रहे हैं।

#### B. `resources/js/app.js` में क्रम से Import करें

JS फ़ाइलें उनके निर्भरता (dependencies) के क्रम में लोड होनी चाहिए: **jQuery $\rightarrow$ Bootstrap $\rightarrow$ Plugins $\rightarrow$ Main Logic**

```javascript
// resources/js/app.js

// CSS Import (Step 2 से)
import '../css/app.css';

// -----------------------------------------------------
// 1. Core Libraries (jQuery & Modernizr)
// Note: आपको यह सुनिश्चित करना होगा कि ये फाइल्स Vite द्वारा सही ढंग से resolved हों।
// मान लें कि Vite resolve aliases (@) का उपयोग करके './vendor' को resources/js/vendor मानता है।

import './vendor/jquery-3.6.0.min.js'; // MUST be first
import './vendor/modernizr-3.11.2.min.js';

// -----------------------------------------------------
// 2. Globalize jQuery (MUST be done after jQuery is loaded)
// यह सुनिश्चित करता है कि अन्य प्लगइन्स $ को पहचानते हैं।
import $ from 'jquery';
window.jQuery = $;
window.$ = $;


// 3. Bootstrap (jQuery के बाद)
import './vendor/bootstrap.bundle.min.js';

// -----------------------------------------------------
// 4. Plugins
import './vendor/isotope.pkgd.min.js';
import './vendor/swiper.min.js';
// import './vendor/all.min.js'; // आपने इसे टिप्पणी आउट (commented out) किया है
import './vendor/wow.js';
import './vendor/counterup.js';
import './vendor/jquery.countdown.min.js';
import './vendor/lightcase.js';
import './vendor/waypoints.min.js';
import './vendor/plugins.js'; // सामान्य प्लगइन फ़ाइल

// -----------------------------------------------------
// 5. Main Custom Logic (MUST be last)
import './vendor/main.js';


// -----------------------------------------------------
// 6. Vue and Inertia Setup
import { createApp, h } from 'vue';
import { createInertiaApp } from '@inertiajs/vue3';
// ... rest of the Inertia setup
```

### स्टेप 4: Vue कॉम्पोनेंट में लॉजिक को स्थानांतरित करें

आपकी फ़ाइल `resources/js/vendor/main.js` में जो भी कस्टम लॉजिक है, उसे सीधे Vue कॉम्पोनेंट में चलने में समस्या हो सकती है।

यदि `main.js` में कोई ऐसा कोड है जो `$(document).ready()` या सीधे `window.onload` पर DOM एलिमेंट्स को मैनीपुलेट करता है (जैसे कि स्लाइडर शुरू करना, काउंटर इनिशियलाइज़ करना):

1.  उस लॉजिक को `main.js` से हटा दें।
2.  `main.js` में एक फंक्शन बनाएं (मान लें `initThemePlugins`)।
3.  अब, उस फ़ंक्शन को अपने Vue कॉम्पोनेंट के `onMounted` हुक के अंदर कॉल करें।

**उदाहरण (`resources/js/Pages/HomePage.vue`):**

```vue
<script setup>
import AppLayout from '@/Layouts/AppLayout.vue';
import { onMounted } from 'vue';
// import initThemePlugins from '@/vendor/main.js' (यदि आपने main.js में function export किया है)

onMounted(() => {
    // 1. यदि आपने main.js से function export किया है:
    // initThemePlugins();

    // 2. या, यदि logic Global चला:
    // आप यहां केवल specific IDs/Classes पर प्लगइन्स को फिर से initialize कर सकते हैं।
    $('.swiper-container').swiper();
    $('.counter').counterup();

    // यह सुनिश्चित करता है कि प्लगइन्स Vue द्वारा रेंडर किए गए DOM पर ही चलें।
});
</script>
```