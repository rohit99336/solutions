`nnjeim/world` पैकेज देशों, राज्यों और शहरों का डेटाबेस आपके Laravel प्रोजेक्ट में जोड़ने का एक और बेहतरीन और आधुनिक तरीका है।

इस पैकेज को इस्तेमाल करने के लिए आपको मुख्य रूप से तीन चरणों का पालन करना होगा:

## 🛠️ `nnjeim/world` पैकेज का उपयोग कैसे करें

### चरण 1: पैकेज इंस्टॉल करें (Install the Package)

अपने Laravel प्रोजेक्ट की रूट डायरेक्टरी में यह Composer कमांड चलाएँ:

```bash
composer require nnjeim/world
php artisan vendor:publish --tag=world
```

*यह कमांड पैकेज को डाउनलोड करेगा और `composer.json` फ़ाइल में एंट्री करेगा।*

### चरण 2: माइग्रेशन चलाएँ (Run Migrations)

पैकेज अब आपके डेटाबेस में आवश्यक टेबल्स (जैसे `countries`, `states`, `cities`) बनाने के लिए तैयार है। यह कमांड चलाएँ:

```bash
php artisan migrate
```

*यह कमांड पैकेज द्वारा प्रदान किए गए सभी माइग्रेशन को चलाएगा और डेटाबेस में खाली टेबल्स बना देगा।*

### चरण 3: डेटा सीड करें (Seed the Data)

टेबल्स बन जाने के बाद, अब आपको उन टेबल्स में वास्तविक देशों, राज्यों और शहरों का डेटा भरना होगा। यह पैकेज एक विशेष कमांड प्रदान करता है जो डेटा को डाउनलोड और सीड करता है:

```bash
php artisan world:seed

or

php artisan db:seed --class=WorldSeeder
```

*यह कमांड World डेटासेट को डाउनलोड करेगा और इसे आपकी `countries`, `states`, और `cities` टेबल्स में इन्सर्ट (insert) करेगा। इसमें कुछ मिनट लग सकते हैं।*

-----

## 🎯 उपयोग (How to Use)

एक बार डेटाबेस में डेटा आ जाने के बाद, आप इस डेटा को एक्सेस करने के लिए सीधे Eloquent मॉडल्स का उपयोग कर सकते हैं।

इस पैकेज में निम्नलिखित मॉडल्स उपलब्ध हैं, जिन्हें आप अपने कंट्रोलर में इस्तेमाल कर सकते हैं:

1.  `Nnjeim\World\Models\Country`
2.  `Nnjeim\World\Models\State`
3.  `Nnjeim\World\Models\City`

### 💡 उदाहरण: सभी देशों को फेच करना (Fetching all Countries)

```php
use Nnjeim\World\Models\Country;

// सभी देशों को प्राप्त करें
$countries = Country::all();

// केवल भारत का पता लगाना
$india = Country::where('name', 'India')->first();
```

### 💡 उदाहरण: राज्यों और शहरों को एक्सेस करना (Accessing States and Cities)

ये मॉडल्स Laravel की Eloquent रिलेशनशिप (Relationships) का उपयोग करते हैं, इसलिए आप आसानी से एक-दूसरे से जुड़े डेटा को एक्सेस कर सकते हैं:

```php
use Nnjeim\World\Models\State;

// किसी देश के सभी राज्यों को प्राप्त करें (उदाहरण: भारत ID 101 है)
$statesOfIndia = Country::find(101)->states;

// किसी राज्य के सभी शहरों को प्राप्त करें (उदाहरण: महाराष्ट्र)
$maharashtra = State::where('name', 'Maharashtra')->first();
$citiesInMaharashtra = $maharashtra->cities;
```

-----